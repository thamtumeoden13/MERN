import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    gridContainer: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'stretch'
    }
}))
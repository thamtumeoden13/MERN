import { makeStyles } from '@mui/styles'

export default makeStyles((theme) => ({
    container: {
        height: '90vh',
        backgroundColor: "white",
        color: "white",
        fontWeight: 'lighter',
        padding: '5px 0',
        marginBottom: 30
    },
}))